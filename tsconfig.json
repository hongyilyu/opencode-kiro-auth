{
  // First typecheck gate for this package. Bun executes the TypeScript directly (no build),
  // so this config is checking-only: noEmit, with Bun's recommended bundler-style resolution.
  "compilerOptions": {
    "lib": ["ESNext"],
    "target": "ESNext",
    "module": "Preserve",
    "moduleResolution": "bundler",
    "moduleDetection": "force",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "noEmit": true,

    "strict": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitOverride": true,
    "skipLibCheck": true,

    // Bun runtime globals + the bun:test module.
    "types": ["bun"]
  },
  // script/ is typechecked but never executed by CI: those are manual, live-credential tools
  // (check-auth, test-model, ...) that spawn opencode against a real account.
  "include": ["index.ts", "src/**/*", "test/**/*", "script/**/*"]
}
